export type Product = {
  id: string;
  name: string;
  price: string;
  imageUrl: string;
  category: string;
}

export type Category = {
  id: string;
  label: string;
  imageUrl: string;
  href: string;
}